// DOM(document object models) Tree

document.getElementsByClassName("<className>").style.color = "red";

// document.head
document.documentElement ;   // for getting html documnet
document.title = "changed Title" ;   // Chage the title of document

console.log(document.body);


// Write the javaScript after the html code because then the html page will have complete access to the html page
